{
  "config": {
    "step": {
      "user": {
        "title": "Codex",
        "description": "Connect Home Assistant to the local Codex worker add-on.",
        "data": {
          "base_url": "Worker URL",
          "api_token": "API token"
        },
        "data_description": {
          "base_url": "The URL of the local Codex CLI Worker add-on API.",
          "api_token": "The API token configured in the Codex CLI Worker add-on options."
        }
      },
      "reauth_confirm": {
        "title": "Codex authentication",
        "description": "Update the Codex worker connection details.",
        "data": {
          "base_url": "Worker URL",
          "api_token": "API token"
        },
        "data_description": {
          "base_url": "The URL of the local Codex CLI Worker add-on API.",
          "api_token": "The API token configured in the Codex CLI Worker add-on options."
        }
      },
      "reconfigure": {
        "title": "Codex",
        "description": "Update the Codex worker connection details.",
        "data": {
          "base_url": "Worker URL",
          "api_token": "API token"
        },
        "data_description": {
          "base_url": "The URL of the local Codex CLI Worker add-on API.",
          "api_token": "The API token configured in the Codex CLI Worker add-on options."
        }
      }
    },
    "error": {
      "cannot_connect": "Could not connect to the Codex worker.",
      "invalid_auth": "The worker rejected the API token."
    },
    "abort": {
      "already_configured": "Codex is already configured.",
      "reauth_successful": "Codex authentication was updated.",
      "reconfigure_successful": "Codex was reconfigured.",
      "unknown": "The Codex config entry could not be found."
    }
  },
  "options": {
    "step": {
      "init": {
        "title": "Codex options",
        "data": {
          "base_url": "Worker URL",
          "api_token": "API token"
        },
        "data_description": {
          "base_url": "The URL of the local Codex CLI Worker add-on API.",
          "api_token": "The API token configured in the Codex CLI Worker add-on options."
        }
      }
    }
  },
  "entity": {
    "sensor": {
      "last_task": {
        "name": "Last task"
      },
      "active_tasks": {
        "name": "Active tasks"
      },
      "auth_status": {
        "name": "Auth status"
      }
    },
    "binary_sensor": {
      "task_running": {
        "name": "Task running"
      }
    }
  },
  "issues": {
    "codex_not_signed_in": {
      "title": "Codex is not signed in",
      "description": "The Codex worker is reachable, but Codex CLI is not signed in. Start the Codex sign-in flow from the add-on web UI or by running the `codex_cli.start_login` action."
    }
  }
}
